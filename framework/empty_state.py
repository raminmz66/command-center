#!/usr/bin/env python3
"""Copy and helpers for the true-empty library CTA."""

TITLE = "No commands yet"
BODY = "Create your first command to fill this launcher."
CTA = "Create command"


def is_library_empty(commands):
    return len(commands) == 0
